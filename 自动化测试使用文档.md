# Android 自动化测试工具使用文档

本文档面向测试人员。工具支持 Midscene 和 Appium 两种测试方式，请根据需要选择其中一种，直接阅读对应章节。

| 测试方式 | 是否需要大模型 | 适用场景 |
| --- | --- | --- |
| Midscene | 需要 | 使用自然语言生成和执行测试，通过模型识别设备画面 |
| Appium | 不需要 | 根据 App 组件树录制和回放，适合稳定、可重复的自动化测试 |

---

# 使用方式一：Midscene

## 1. Midscene 使用条件

使用 Midscene 需要：

- Node.js 18 或更高版本。
- npm。
- Android SDK 和 ADB。
- 开启 USB 调试的 Android 设备。
- 可用的多模态大模型配置。

Midscene 不需要安装或启动 Appium。

## 2. 准备运行环境

### 2.1 检查 Node.js

```sh
node -v
npm -v
```

### 2.2 检查 ADB

```sh
adb version
```

如果系统无法识别 `adb`，启动工具后可在“参数配置 > 运行配置”中填写 Android SDK 根目录。该目录下应包含：

```text
platform-tools/adb
```

### 2.3 连接 Android 设备

1. 在手机中开启“开发者选项”和“USB 调试”。
2. 使用 USB 连接手机和电脑。
3. 手机出现调试授权时点击“允许”。
4. 执行：

```sh
adb devices -l
```

设备状态应为 `device`。如果显示 `unauthorized`，请重新确认手机上的 USB 调试授权。

## 3. 启动 Midscene 测试工具

在终端执行：

```sh
npx --yes android-midscene-automation@latest
```

启动成功后，在浏览器访问：

```text
http://127.0.0.1:5173/
```

如果 `5173` 端口已被占用：

```sh
npx --yes android-midscene-automation@latest --port 5174
```

然后访问 `http://127.0.0.1:5174/`。

每位测试人员都需要在自己的电脑上运行工具，页面识别的是当前电脑通过 ADB 连接的手机。

停止运行时，在终端按 `Ctrl + C`。

## 4. 配置 Midscene

进入“参数配置”菜单。

### 4.1 运行配置

- `Android SDK 路径`：可选。系统无法自动找到 ADB 时填写 Android SDK 根目录。
- `回放报告目录`：仅 Appium 使用，Midscene 测试可以不填写。

### 4.2 预设 App 参数

建议添加待测试 App：

- 应用名称：便于识别的名称。
- App 包名：例如 `com.example.app`。

### 4.3 Midscene 模型

Midscene 必须配置模型。可以选择：

- `自定义提供方`：填写 Base URL、API Key、Model Name 和 Model Family。
- `使用 Codex`：按页面要求配置 Codex 模型。

填写后点击“测试模型”，确认模型可用。

### 4.4 AI 生成脚本模型

需要使用 AI 生成或优化测试脚本时配置。填写完成后建议先执行模型测试。

## 5. 使用 AI 生成测试脚本

1. 确认 Android 设备已经连接。
2. 确认 Midscene 模型测试通过。
3. 进入 `Midscene > 测试脚本生成`。
4. 填写测试需求，或导入支持的测试用例文件。
5. 选择设备和预设 App。
6. 点击生成脚本。
7. 检查生成的操作步骤、输入内容和验证条件。
8. 保存脚本。

测试需求应明确说明：

- 目标 App 和目标页面。
- 前置状态，例如“当前位于系统桌面”或“用户未登录”。
- 操作步骤和测试数据。
- 成功条件和失败条件。

示例：

```text
打开目标 App，进入账号密码登录页。
在账号输入框输入测试账号，在密码输入框输入测试密码。
勾选用户协议后点击登录。
等待首页出现，并验证首页底部导航可见。
```

## 6. 执行 Midscene 脚本

1. 进入 `Midscene > 自动化测试`。
2. 选择已保存的脚本和 Android 设备。
3. 确认模型配置有效。
4. 点击“开始执行”。
5. 在“执行输出”中查看实时日志。
6. 需要中止时点击“停止执行”。

Midscene 会调用多模态模型理解设备画面，执行速度受模型响应、网络状态、页面变化和步骤数量影响。

## 7. Midscene 使用建议

- 一条 AI 指令尽量只完成一个明确目标。
- 输入账号、密码等操作使用独立步骤。
- 不要在一个步骤中同时处理大量弹窗和业务操作。
- 等待页面时描述可观察到的成功标志，不要只写“等待完成”。
- 生成后检查测试数据、按钮名称和成功条件是否正确。
- 脚本较慢时，减少重复的 AI 判断和过于宽泛的页面描述。

## 8. Midscene 常见问题

### 8.1 页面没有识别到设备

执行 `adb devices -l`，确认设备状态为 `device`，然后刷新页面或重新选择设备。

### 8.2 模型测试失败

检查 Base URL、API Key、Model Name、Model Family，以及当前电脑是否可以访问模型服务。

### 8.3 执行速度较慢

减少重复判断、缩小单个步骤的目标范围，并使用明确的等待条件。组件 ID 稳定且需要高频回放时，可以改用 Appium。

### 8.4 设备画面没有更新

确认手机亮屏并解锁、ADB 连接正常，然后点击设备预览中的刷新按钮或重新选择设备。

---

# 使用方式二：Appium

## 1. Appium 使用条件

使用 Appium 需要：

- Node.js 18 或更高版本。
- npm。
- Android SDK 和 ADB。
- 开启 USB 调试的 Android 设备。
- Appium 3.x 和 UiAutomator2 Driver。

Appium 不依赖任何大模型，不需要填写 Base URL、API Key、Model Name 或 Model Family。

## 2. 准备运行环境

### 2.1 检查 Node.js、ADB 和设备

```sh
node -v
npm -v
adb version
adb devices -l
```

设备状态应为 `device`。如果显示 `unauthorized`，请在手机上重新允许 USB 调试。

如果系统无法识别 `adb`，启动工具后可在“参数配置 > 运行配置”中填写包含 `platform-tools/adb` 的 Android SDK 根目录。

### 2.2 安装 Appium

```sh
npm install -g appium@3.5.0
appium driver install uiautomator2
```

检查安装结果：

```sh
appium --version
appium driver list --installed
```

如果提示 UiAutomator2 已经安装，不需要重复安装。

## 3. 启动 Appium 测试工具

打开第一个终端，启动 Appium：

```sh
appium
```

保持该终端运行。打开第二个终端，启动测试工具：

```sh
npx --yes android-midscene-automation@latest
```

启动成功后访问 `http://127.0.0.1:5173/`。

如果端口被占用：

```sh
npx --yes android-midscene-automation@latest --port 5174
```

停止运行时，分别在测试工具和 Appium 的终端按 `Ctrl + C`。

## 4. 配置 Appium

进入“参数配置”菜单。

### 4.1 运行配置

- `Android SDK 路径`：可选。系统无法自动找到 ADB 时填写 Android SDK 根目录。
- `回放报告目录`：可选。不填写时使用启动目录下的 `output`。

### 4.2 预设 App 参数

Appium 录制前必须添加待测试 App：

- 应用名称：便于识别的名称。
- App 包名：例如 `com.example.app`。

Appium 页面只能从已经保存的预设 App 中选择。模型配置与 Appium 无关，可以保持为空。

## 5. 进入 Appium 录制页面

1. 确认 Appium 服务正在运行。
2. 确认手机已经通过 ADB 连接。
3. 确认已添加预设 App。
4. 进入 `Appium` 菜单。
5. 选择设备和预设 App。

页面会显示 App 组件树、设备预览、录制流程、已录制脚本列表和回放输出。

## 6. 选择 App 组件

可以通过两种方式选择组件：

1. 点击设备预览中的目标组件，组件树会自动定位对应节点。
2. 直接在 App 组件树中点击节点。

选择后可以查看 resource-id、组件文本、类型、bounds、selector 和当前 Activity。

页面或 Activity 变化后，组件树会自动刷新。需要立即刷新时，点击组件树标题旁的刷新按钮。

## 7. 创建录制流程

1. 在开始节点下点击“插入操作”。
2. 从操作列表中选择需要的操作。
3. 需要操作组件时，先在预览或组件树中选择组件。
4. 在任意节点后的“插入操作”中添加后续步骤。
5. 点击节点展开详细配置。
6. 为节点填写备注，例如“账号输入框”“登录按钮”。
7. 检查流程后填写脚本名称并保存。

流程画布支持：

- 按住节点或空白区域拖动画布。
- 按住 `Ctrl` 并滚动鼠标滚轮进行缩放。
- 点击放大按钮查看和编辑完整流程。
- 在已有步骤中间插入操作。
- 删除不需要的节点。
- 再次点击已展开的节点关闭配置。

## 8. Appium 操作说明

| 操作 | 用途 |
| --- | --- |
| 启动 App | 启动当前预设 App，只能从开始节点添加，每个脚本最多一个 |
| 录制点击 | 点击按钮、图标、列表项等组件 |
| 录制输入 | 向选中的输入框输入文本，录制后可以修改内容 |
| 清空输入 | 清除输入框已有内容 |
| 点击坐标 | 按指定坐标点击 |
| 长按 | 长按选中的组件或指定位置 |
| 系统返回 | 执行 Android 返回键 |
| Home 键 | 返回系统桌面 |
| 最近任务 | 打开最近任务页面 |
| 电源键 | 执行设备电源键操作 |
| 滑动 | 向上、向下、向左或向右滑动 |
| 双指缩放 | 对页面执行放大或缩小手势 |
| 添加延时 | 在两个步骤之间等待指定时间 |
| 判断存在 | 判断组件或指定文本是否存在，并进入“是/否”分支 |
| 存在则点击 | 组件存在时点击，不存在时跳过 |
| 存在则输入 | 输入框存在时输入，不存在时跳过 |
| 存在则清空 | 输入框存在时清空，不存在时跳过 |
| 存在则返回 | 目标存在时执行返回，不存在时跳过 |
| 等待出现 | 等待目标组件出现，超时后失败 |
| 断言存在 | 验证目标组件必须存在 |
| 断言文本 | 验证页面或组件文本符合预期 |
| 等待元素消失 | 等待加载框、弹窗等目标消失 |
| 等待 Activity | 等待页面进入指定 Activity |
| 连接脚本 | 当前脚本完成后继续执行另一个脚本 |

## 9. 判断可选弹窗

弹窗不是每次都出现时，使用“判断存在”，不要直接使用“断言存在”。

1. 在组件树中选择弹窗或弹窗内组件。
2. 添加“判断存在”。
3. 多个弹窗使用相同组件 ID 时，在节点配置中填写指定文本。
4. 根据需要选择精准匹配或模糊匹配。
5. 在“是”分支中添加关闭、确认或输入操作。
6. “否”分支可以为空，或连接到后续主流程。

```text
判断“发现新版本”是否存在
├─ 是：点击“稍后再说”
└─ 否：继续后续步骤
```

## 10. 连接分支和后续节点

- 点击分支中的“插入操作”，在对应分支添加步骤。
- 需要回到后续主流程时，点击“连接到下一节点”。
- 建立连接后按钮会变为“取消连接”。
- 后续没有主流程节点时，不显示连接按钮。

## 11. 单 Activity 录制和连接脚本

一个脚本只录制一个 Activity。点击导致 Activity 变化时，系统会提示保存当前脚本。保存后，在新 Activity 中创建另一个脚本，再通过“连接脚本”组合多个页面。

```text
登录页脚本
  -> 点击登录
  -> 连接“首页脚本”

首页脚本
  -> 操作首页组件
```

前一个脚本必须能够让页面真正进入后一个脚本的入口 Activity。

## 12. 保存和管理 Appium 脚本

- 新脚本：填写脚本名称后点击“保存”。
- 更新脚本：从脚本列表加载后修改，再次点击“保存”。
- 删除脚本：删除不再使用的脚本。
- 下载脚本：导出脚本文件进行备份或共享。
- 导入脚本：导入其他人导出的脚本文件。

有未保存修改时刷新或关闭页面，浏览器会显示保存提醒。

## 13. 回放 Appium 脚本

1. 确认 Appium 服务正在运行。
2. 选择 Android 设备和预设 App。
3. 在脚本列表中选择脚本。
4. 确认手机当前页面与脚本入口 Activity 相符。
5. 点击“回放”。
6. 在回放输出中查看节点执行信息。

默认情况下，工具会为每次回放启动独立的 Appium 服务。回放输出会实时显示节点执行信息，并完整保留 Appium、AndroidUiautomator2Driver 和 ADB 的服务端日志，包括请求、响应和驱动诊断信息。

详细日志可能包含输入值和设备信息，共享 `.log` 文件前请检查并清理敏感内容。显式设置 `APPIUM_SERVER_URL` 连接外部服务时，工具无法读取外部终端的原始日志，只记录 WebDriver 接口级信息。

回放过程中顶部会显示“终止”按钮。点击后会停止后续节点并关闭当前 Appium session，本次执行仍会生成报告、日志和截图回放，结果显示为“已终止”。

回放结束后会生成 Markdown 报告、同名 `.log` 文件和单文件 `.html` 截图回放。报告包括设备、App、Activity、节点配置、执行结果、判断分支和错误日志；`.log` 文件保存完整回放输出与托管 Appium 进程未经截断的 stdout/stderr。

HTML 回放会保存每个实际执行节点前后的设备截图，判断、失败和手动终止也会保留对应画面。打开 HTML 后，左侧显示步骤与默认收起的完整日志，顶部时间轴按真实执行时间排列截图；可以点击时间轴跳转，或通过播放、上一帧、下一帧和进度条连续回放。截图已经内嵌，不需要复制额外图片目录。

报告默认保存在启动目录的 `output` 文件夹，也可以在“参数配置 > 运行配置”中修改位置。

## 14. Appium 常见问题

### 14.1 页面没有识别到设备

执行 `adb devices -l`，确认设备状态为 `device`。每位测试人员必须在连接手机的电脑上启动工具。

### 14.2 设备预览或组件树没有更新

确认手机亮屏并解锁、ADB 连接正常，然后点击对应区域的刷新按钮。录制或回放进行中时，组件树会暂时停止自动刷新。

### 14.3 账号框和密码框使用相同 ID

录制器会保存父级上下文 selector，回放时先定位父级布局，再定位内部输入框。建议分别添加“账号输入框”和“密码输入框”备注。

### 14.4 可选弹窗导致回放失败

使用“判断存在”或“存在则点击”。相同组件 ID 的弹窗应配置指定文本和匹配方式。

### 14.5 连接脚本提示 Activity 不匹配

当前页面还没有进入后一个脚本的入口 Activity。检查前一个脚本是否包含必要的点击、输入、等待 Activity 或登录成功条件。

### 14.6 出现 `UiAutomation not connected`

1. 等待组件树刷新完成后再回放。
2. 停止重复运行的 Appium session。
3. 确认只启动了一个 Appium 服务。
4. 重新连接设备并刷新组件树。
5. 必要时重启 Appium 和手机 USB 调试连接。

### 14.7 创建 Appium Session 失败

确认 Appium 正在运行、UiAutomator2 Driver 已安装、设备状态为 `device`、App 包名正确，并且手机没有被其他自动化工具占用。

## 15. Appium 推荐录制规范

- 一个脚本只负责一个 Activity 或明确业务目标。
- 多个 Activity 通过“连接脚本”组合。
- 节点备注使用业务名称，不要只保留组件 ID。
- 使用专用测试数据，不要在共享脚本中保存真实敏感信息。
- 关键操作后增加断言。
- 对非必现弹窗使用判断分支。
- 保存后先单独回放，再组合完整流程。
- 失败后优先查看回放日志和 Markdown 报告中的失败节点。
